import React from "react";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";
import * as ReactTilt from 'react-tilt';

const Tilt = ReactTilt.Tilt;


const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className='bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt='web-development'
          className='w-16 h-16 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]'
      >
        I had the privilege of completing a MERN Stack Development internship at 
        EMRChains NSTP NUST Islamabad, where I developed dynamic, scalable appli
        cations using MongoDB, Express, React, and Node.js. Additionally, during 
        my internship at Ecode Camp Pvt Ltd, I focused on frontend development, 
        crafting intuitive and visually engaging web interfaces.I also gained 
        hands-on experience as a Python Intern at RapidEv, NSTP NUST Islamabad, 
        where I deepened my under standing of Python and worked on innovative 
        projects. This role reinforced my expertise in Python, and I continue 
        to explore new techniques and best practices to stay ahead in this ever
        evolving field!
      </motion.p>

      <div className='mt-20 flex flex-wrap gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
